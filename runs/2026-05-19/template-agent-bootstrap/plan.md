# Plan

1. Inspect WIP and required source files.
2. Read `AGENTS.md`, all existing template files, and `CSS/SPW.md`.
3. Search for `strongdm.md` and Excel sources.
4. Replace `template/REQUESTS.md` with an execution-ready seed for CRM dashboard agents.
5. Add `template/AGENTS.md` using the required `**Mục tiêu:**` structure.
6. Add/update template scripts so future agents have build/deploy entrypoints.
7. Validate required files, script syntax, and key content markers.
8. Save run trace and final result.

