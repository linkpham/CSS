# Input

Human requested:

- Read and understand the project in `template`, `strongdm.md`, and `AGENTS.md`.
- Create `template/REQUESTS.md` with instructions so a coding agent can start from that file.
- Create `template/AGENTS.md` beginning with:

```md
**Mục tiêu:**
```

- The template should instruct future agents to use data from `excels/` and the CSS SPW spec to analyze business workflow, data structure, KPI, relationships, and build a production-ready CRM dashboard.
- Ensure template structure:

```text
template/
├── docs/
│   ├── regression/
│   ├── specs/
│   └── runbooks/
└── scripts/
```

Relevant runtime observation:

- `strongdm.md` was not present in the current repo.
- `excels/` was not present in the current repo.
- `CSS/SPW.md` and `CSS/spw.md` were present.

