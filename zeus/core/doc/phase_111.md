# Phase 111

- Chỉnh sửa `zeus-git-local.sh` khi commit hoặc push thì sẽ theo .gitignore là nội dung theo `gitignore-local.txt`
- Chỉnh sửa `zeus-git-galaxy.sh` khi commit hoặc push thì sẽ theo .gitignore là nội dung theo `gitignore-galaxy.txt`. Tuy nhiên sau khi push xong thì phải trả lại .gitigore là nội dung trong `gitignore-local.txt`. 
